import { ToggleCommentsDirective } from './toggle-comments.directive';

describe('ToggleCommentsDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleCommentsDirective();
    expect(directive).toBeTruthy();
  });
});
